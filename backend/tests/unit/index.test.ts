/**
 * Test suite index - run all unit tests
 * 
 * This file serves as a reference for all test files:
 * - ComputeComparison.test.ts - Tests for compliance comparison calculations
 * - ComputeCB.test.ts - Tests for Compliance Balance (CB) calculations
 *   - Positive CB (surplus scenarios)
 *   - Negative CB (deficit scenarios)
 *   - Zero CB edge cases
 *   - Formula verification
 * - BankSurplus.test.ts - Tests for banking surplus compliance units
 *   - Basic banking operations
 *   - Over-banking scenarios (capacity limits)
 *   - Edge cases (zero, negative, fractional)
 * - ApplyBanked.test.ts - Tests for applying banked units to cover deficits
 *   - Successful applications
 *   - Insufficient units scenarios
 *   - Expired units handling
 *   - FIFO ordering
 * - CreatePool.test.ts - Tests for pool creation
 *   - Successful pool creation
 *   - Mis-pool validation errors (misconfigured pools)
 *   - Edge cases
 */

// This file is for documentation purposes only
// All tests are in their respective .test.ts files

export {};

